spring:
  component-test:
    infrastructure:
      load: ALWAYS
      db:
        postgres:
          image: postgres:alpine
          scripts:
            - sql/test_database.sql
  datasource:
    name: test_database
    url: jdbc:postgresql://localhost:5432/test_database
    username: postgres
    password: postgres
